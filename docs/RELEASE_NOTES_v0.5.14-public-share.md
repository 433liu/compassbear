# compass-bear v0.5.14-public-share

This earlier public-share package has been superseded by `v0.5.14-public`.

Use the current release notes instead:

- `docs/RELEASE_NOTES_v0.5.14-public.md`

The current public package removes project-specific example claims and domain-specific test prompts that could reveal the user's manuscript direction.
